# Ticket 048: Reconcile ticket-047 governance record

- **ID**: ticket-048
- **Owner**: unresolved:human
- **Status**: IN_PROGRESS
- **Workflow state**: VALIDATION
- **Created**: 2026-08-05

## Goal and scope

Restore the governance record of ticket-047, which was lost while its
implementation commit was transplanted onto `main`. The cherry-pick resolved a
`TODO.md` conflict with `--ours`, so the completed ticket was never registered
in either governance index, and the ticket directory was published without the
two scaffold files the manifest declares as required.

Concretely this ticket:

- registers ticket-047 under `## Completed tickets` in `TODO.md`,
- regenerates the `project/TICKETS.md` index so ticket-047 has a complete row,
- adds the missing `project/ticket-047/preprompt.md` and
  `project/ticket-047/ai-codex-logs.txt`.

This is a documentation and evidence reconciliation only. No source file, test,
script, workflow or dependency is touched, and ticket-047 stays `DONE`.

## Acceptance criteria

- [x] AC-01: Scope is approved by a human owner.
- [x] AC-02: `TODO.md` lists ticket-047 under `## Completed tickets` with its
  delivered outcome and its publication route, and `## Active tickets` reflects
  only this ticket.
- [x] AC-03: `project/TICKETS.md` is regenerated by `project/readme.sh` and the
  ticket-047 row links `README.md`, `preprompt.md`, `ai-codex.md`,
  `ai-codex-logs.txt` and `changelog.md` without a `-` placeholder.
- [x] AC-04: `project/ticket-047/` satisfies the manifest's `requiredFiles` and
  `requiredAgentFiles`, and the restored files describe only what ticket-047
  actually did — no invented approval, review or attestation evidence.
- [x] AC-05: `make governance` passes and `make verify` stays green, with no
  change under `src/**`, `scripts/**`, `test/**`, `.github/**` or
  `.governance/**`.

## Participants

- Human participant: unresolved; no user-* file was created by this script.
- Agent participant: [ai-claude.md](ai-claude.md)

## Architecture and bounds

- Component 1: the governance indexes `TODO.md` and `project/TICKETS.md`, where
  `project/TICKETS.md` is regenerated by the managed `project/readme.sh` wrapper
  rather than edited by hand.
- Component 2: the ticket-047 evidence directory, which gains the two scaffold
  files the manifest requires of every ticket directory.
- Complexity class: XS; maximum 30 minutes, four governance files, two affected
  components, no public interface, runtime dependency or behavior change.

## Non-goals

- No edit to `scripts/github-event-log.mjs`, its tests, fixtures or
  `docs/EVENT_LOG_DSL.md`; ticket-047's implementation is closed.
- No `.github/workflows/**` wiring of the acquisition script; that remains
  deferred to a separate governance ticket.
- No rewrite of ticket-047's status, acceptance criteria or approval boundary,
  and no fabricated Koru/Validator approval or PR-merge evidence.
- No change to `.governance/**`, the manifest or the ticket scaffolder.

## Approval boundary

The human owner selected this reconciliation scope on 2026-08-05, choosing it
over wiring the acquisition script into GitHub Actions. That authorizes the
governance-index and ticket-047 scaffold boundary declared in `intent.json`.

Ticket-047's implementation commits are published on branch
`ticket/047-event-log-acquisition` as pull request #64, because branch
protection on `main` rejects direct pushes. This ticket branches from that head
so the ticket-047 files it reconciles are present.
