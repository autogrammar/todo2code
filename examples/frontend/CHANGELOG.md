# Changelog

## 0.2.0

- Dodano klienta HTTP `fetchEvents` w `src/api.ts`.
- Dodano renderowanie tabeli rozbieżności w `src/render.ts`.
- Dodano montowanie panelu w `src/app.ts`.

## 0.1.0

- Inicjalny szkielet panelu.
